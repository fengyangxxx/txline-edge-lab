{
  "name": "txline-edge-lab",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "live-check": "node scripts/live-contract-check.mjs",
    "packet": "node scripts/export-packet.mjs",
    "scorecard": "node scripts/judge-scorecard.mjs",
    "smoke": "node scripts/smoke.mjs",
    "verify": "node scripts/verify-packet.mjs"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
